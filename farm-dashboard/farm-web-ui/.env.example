# Backend API URL
VITE_API_URL=http://localhost:8080/api/alerts

# Admin credentials (client-side only - NOT secure for production)
# For production, implement server-side authentication
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=changeme
